# 📦 Order Management System - Filament Blueprint

## 🎯 Overview

ระบบจัดการออเดอร์ที่สร้างด้วย Filament 4.5 และ Laravel 12 โดยมีฟีเจอร์ครบถ้วนสำหรับการจัดการออเดอร์, ลูกค้า, และสินค้า

---

## 📁 โครงสร้างไฟล์

```
app/
├── Enums/
│   └── OrderStatus.php              # Enum สำหรับสถานะออเดอร์
├── Models/
│   ├── Customer.php                 # Model ลูกค้า
│   ├── Product.php                  # Model สินค้า
│   ├── Order.php                    # Model ออเดอร์
│   └── OrderItem.php                # Model รายการสินค้าในออเดอร์
├── Policies/
│   └── OrderPolicy.php              # Policy สำหรับ authorization
├── Providers/
│   └── AppServiceProvider.php       # Register policies
└── Filament/
    └── Resources/
        └── Orders/
            ├── OrderResource.php    # Resource class
            ├── Schemas/
            │   └── OrderForm.php    # Form Schema
            ├── Tables/
            │   └── OrdersTable.php # Table Schema
            └── Pages/
                ├── ListOrders.php   # List page
                ├── CreateOrder.php  # Create page
                └── EditOrder.php    # Edit page

database/
└── migrations/
    ├── create_customers_table.php
    ├── create_products_table.php
    ├── create_orders_table.php
    └── create_order_items_table.php
```

---

## 🗄️ Database Schema

### Customers Table
- `id` - Primary key
- `name` - ชื่อลูกค้า
- `email` - อีเมล
- `phone` - เบอร์โทร
- `address` - ที่อยู่
- `timestamps`

### Products Table
- `id` - Primary key
- `name` - ชื่อสินค้า
- `sku` - รหัสสินค้า (unique)
- `description` - คำอธิบาย
- `price` - ราคา
- `is_active` - สถานะเปิดใช้งาน
- `timestamps`

### Orders Table
- `id` - Primary key
- `customer_id` - Foreign key to customers
- `order_number` - เลขที่ออเดอร์ (unique, auto-generated)
- `status` - สถานะออเดอร์ (enum: pending, confirmed, shipped, delivered, cancelled)
- `shipping_address` - ที่อยู่จัดส่ง
- `notes` - หมายเหตุ
- `subtotal` - ยอดรวม
- `total` - ยอดรวมทั้งหมด
- `timestamps`

### Order Items Table
- `id` - Primary key
- `order_id` - Foreign key to orders
- `product_id` - Foreign key to products
- `quantity` - จำนวน
- `unit_price` - ราคาต่อหน่วย
- `line_total` - รวม (quantity × unit_price)
- `timestamps`

---

## 🔄 Relationships

### Order Model
```php
// Order belongs to Customer
public function customer(): BelongsTo

// Order has many OrderItems
public function orderItems(): HasMany
```

### OrderItem Model
```php
// OrderItem belongs to Order
public function order(): BelongsTo

// OrderItem belongs to Product
public function product(): BelongsTo
```

### Customer Model
```php
// Customer has many Orders
public function orders(): HasMany
```

### Product Model
```php
// Product has many OrderItems
public function orderItems(): HasMany
```

---

## ✨ Features

### 1. Order Status Management
- **Pending** - รอดำเนินการ (สี warning)
- **Confirmed** - ยืนยันแล้ว (สี info)
- **Shipped** - จัดส่งแล้ว (สี primary)
- **Delivered** - ส่งถึงแล้ว (สี success)
- **Cancelled** - ยกเลิก (สี danger)

### 2. Auto-calculation
- **Line Total**: คำนวณอัตโนมัติเมื่อเลือกสินค้า, เปลี่ยนจำนวน, หรือเปลี่ยนราคา
  - `line_total = quantity × unit_price`
- **Order Totals**: คำนวณอัตโนมัติจาก order items
  - `subtotal = sum of all line_totals`
  - `total = subtotal` (สามารถเพิ่ม tax, shipping ได้ในอนาคต)

### 3. Search & Filters
- **Search by Customer Name**: ค้นหาออเดอร์ด้วยชื่อลูกค้า
- **Filter by Status**: กรองตามสถานะออเดอร์ (สามารถเลือกหลายสถานะ)
- **Filter by Date Range**: กรองตามช่วงวันที่สร้างออเดอร์

### 4. Authorization
- **Delete Orders**: เฉพาะ admin เท่านั้นที่สามารถลบออเดอร์ได้
- **Bulk Delete**: เฉพาะ admin เท่านั้นที่สามารถลบหลายออเดอร์พร้อมกันได้

### 5. Business Rules
- **Cancel Restriction**: ไม่สามารถยกเลิกออเดอร์ที่จัดส่งแล้ว (shipped, delivered)
- **Order Number**: สร้างอัตโนมัติในรูปแบบ `ORD-000001`
- **Customer Creation**: สามารถสร้างลูกค้าใหม่ได้จากฟอร์มออเดอร์

---

## 📝 การใช้งาน

### 1. สร้างออเดอร์ใหม่
1. ไปที่ `/admin/orders` หรือคลิกเมนู "ออเดอร์"
2. คลิกปุ่ม "สร้างออเดอร์ใหม่"
3. เลือกหรือสร้างลูกค้าใหม่
4. เพิ่มรายการสินค้า (สามารถเพิ่มหลายรายการ)
5. กรอกที่อยู่จัดส่ง
6. เลือกสถานะ (default: Pending)
7. คลิก "สร้าง"

**หมายเหตุ**: 
- เลขที่ออเดอร์จะถูกสร้างอัตโนมัติ
- ยอดรวมจะคำนวณอัตโนมัติเมื่อเพิ่ม/แก้ไขรายการสินค้า

### 2. แก้ไขออเดอร์
1. คลิกปุ่ม "แก้ไข" ในแถวที่ต้องการ
2. แก้ไขข้อมูลตามต้องการ
3. **ข้อจำกัด**: ไม่สามารถเปลี่ยนสถานะเป็น "ยกเลิก" ถ้าออเดอร์จัดส่งแล้ว
4. คลิก "บันทึก"

### 3. ลบออเดอร์
- เฉพาะ admin เท่านั้นที่เห็นปุ่มลบ
- คลิกปุ่ม "ลบ" และยืนยันการลบ

### 4. ค้นหาและกรอง
- **ค้นหา**: พิมพ์ชื่อลูกค้าในช่องค้นหา
- **กรองสถานะ**: คลิก filter และเลือกสถานะที่ต้องการ
- **กรองวันที่**: คลิก filter และเลือกช่วงวันที่

---

## 🔧 Configuration

### Policy Configuration

ใน `app/Policies/OrderPolicy.php`:
- ปรับเงื่อนไข `delete()` และ `deleteAny()` ตามระบบ role ของคุณ
- ปัจจุบันใช้: `$user->email === 'admin@example.com' || $user->is_admin ?? false`

**ตัวอย่างการปรับแต่ง**:
```php
// ถ้าใช้ role-based system
public function delete(User $user, Order $order): bool
{
    return $user->hasRole('admin');
}

// หรือใช้ Spatie Permission
public function delete(User $user, Order $order): bool
{
    return $user->can('delete orders');
}
```

### User Model

เพิ่ม field `is_admin` ใน users table (ถ้ายังไม่มี):

```bash
php artisan make:migration add_is_admin_to_users_table
```

Migration:
```php
Schema::table('users', function (Blueprint $table) {
    $table->boolean('is_admin')->default(false);
});
```

---

## 🎨 Form Features

### Order Form Components

1. **Customer Select**
   - Searchable dropdown
   - สามารถสร้างลูกค้าใหม่ได้ทันที
   - Preload สำหรับ performance

2. **Order Items Repeater**
   - เพิ่ม/ลบรายการสินค้าได้หลายรายการ
   - Auto-fill price เมื่อเลือกสินค้า
   - Auto-calculate line total
   - Collapsible items

3. **Status Select**
   - Dropdown แสดงสถานะทั้งหมด
   - Validation: ไม่สามารถยกเลิกถ้าจัดส่งแล้ว

---

## 🔍 Table Features

### Columns
- เลขที่ออเดอร์ (searchable, sortable)
- ชื่อลูกค้า (searchable, sortable)
- สถานะ (editable dropdown)
- ยอดรวม (formatted as money, sortable)
- จำนวนรายการ (counts order items)
- วันที่สร้าง (sortable, toggleable)

### Filters
- สถานะ (multiple select)
- ช่วงวันที่สร้าง

### Actions
- แก้ไข (ทุกคน)
- ลบ (เฉพาะ admin)

---

## 🚀 การติดตั้ง

### 1. Run Migrations
```bash
php artisan migrate
```

### 2. Create Sample Data (Optional)
สร้าง Seeder สำหรับทดสอบ:
```bash
php artisan make:seeder OrderSeeder
```

### 3. Access the System
- ไปที่ `/admin/orders`
- หรือคลิกเมนู "ออเดอร์" ใน sidebar

---

## 📊 Example Usage Flow

### Scenario: สร้างออเดอร์ใหม่

1. **Customer**: เลือก "John Doe" หรือสร้างใหม่
2. **Order Items**: 
   - เพิ่ม Product A จำนวน 2 ชิ้น ราคา 100 บาท → Line Total: 200
   - เพิ่ม Product B จำนวน 1 ชิ้น ราคา 500 บาท → Line Total: 500
3. **Totals**: 
   - Subtotal: 700 บาท
   - Total: 700 บาท
4. **Status**: Pending
5. **Save**: ออเดอร์ถูกสร้างพร้อมเลขที่ `ORD-000001`

### Scenario: อัปเดตสถานะ

1. แก้ไขออเดอร์ `ORD-000001`
2. เปลี่ยนสถานะเป็น "Confirmed" → ✅ สำเร็จ
3. เปลี่ยนสถานะเป็น "Shipped" → ✅ สำเร็จ
4. เปลี่ยนสถานะเป็น "Cancelled" → ❌ Error: "ไม่สามารถยกเลิกออเดอร์ที่จัดส่งแล้วได้"

---

## 🔐 Security & Authorization

### OrderPolicy Methods

- `viewAny()` - ทุกคนสามารถดูรายการออเดอร์ได้
- `view()` - ทุกคนสามารถดูออเดอร์ได้
- `create()` - ทุกคนสามารถสร้างออเดอร์ได้
- `update()` - ทุกคนสามารถแก้ไขออเดอร์ได้
- `delete()` - เฉพาะ admin เท่านั้น
- `deleteAny()` - เฉพาะ admin เท่านั้น

---

## 🎓 Key Concepts

### 1. Eloquent Relationships
- `belongsTo()` - Order belongs to Customer
- `hasMany()` - Order has many OrderItems

### 2. Model Events
- `boot()` method ใน OrderItem เพื่อคำนวณ line_total อัตโนมัติ
- `calculateTotals()` ใน Order เพื่อคำนวณยอดรวม

### 3. Filament Form Components
- `Repeater` - สำหรับรายการสินค้าที่มีหลายรายการ
- `live()` - สำหรับ real-time updates
- `afterStateUpdated()` - สำหรับ auto-calculation

### 4. Filament Table Features
- `searchable()` - ค้นหาได้
- `sortable()` - เรียงลำดับได้
- `filters()` - กรองข้อมูล
- `modifyQueryUsing()` - แก้ไข query

---

## 🐛 Troubleshooting

### ปัญหา: ไม่เห็นปุ่มลบ
**แก้ไข**: ตรวจสอบว่า user เป็น admin หรือไม่ (ดู Policy)

### ปัญหา: ไม่สามารถยกเลิกออเดอร์
**แก้ไข**: ตรวจสอบสถานะปัจจุบัน - ไม่สามารถยกเลิกถ้า shipped หรือ delivered แล้ว

### ปัญหา: ยอดรวมไม่ถูกต้อง
**แก้ไข**: ตรวจสอบว่า order items มี line_total หรือไม่

---

## 📚 เอกสารเพิ่มเติม

- [Filament Documentation](https://filamentphp.com/docs)
- [Laravel Eloquent Relationships](https://laravel.com/docs/eloquent-relationships)
- [Laravel Policies](https://laravel.com/docs/authorization#creating-policies)
