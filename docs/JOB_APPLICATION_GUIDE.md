# 📋 คู่มือใบรับสมัครงาน - ตัวอย่าง Form Components ครบถ้วน

## 🎯 ภาพรวม

ใบรับสมัครงานนี้เป็นตัวอย่างที่แสดงการใช้ **Form Components ครบถ้วน** ใน Filament 4.5 โดยแบ่งเป็น **2 Form**:

1. **Form 1: ข้อมูลส่วนตัว** - ใช้ Components: TextInput, DatePicker, Select, Textarea, Radio, Toggle, Checkbox
2. **Form 2: ข้อมูลเพิ่มเติม** - ใช้ Components: Textarea, TextInput, FileUpload, Select (Multiple), Toggle

---

## 📝 Form 1: ข้อมูลส่วนตัว (Personal Information)

### Components ที่ใช้:

#### 1. **TextInput** - รับข้อความสั้นๆ

```php
TextInput::make('first_name')
    ->label('ชื่อ')
    ->required()
    ->maxLength(100)
    ->placeholder('กรุณากรอกชื่อ')
```

**คุณสมบัติ:**
- `->email()` - สำหรับอีเมล (มี validation)
- `->tel()` - สำหรับเบอร์โทร
- `->numeric()` - สำหรับตัวเลข
- `->mask('999-999-9999')` - กำหนดรูปแบบการกรอก
- `->prefix('฿')` - เพิ่มสัญลักษณ์ด้านหน้า
- `->suffix('%')` - เพิ่มสัญลักษณ์ด้านหลัง
- `->unique()` - ตรวจสอบความซ้ำ (สำหรับอีเมล)

**ประเภทข้อมูล:** `string`, `int`, `float`

---

#### 2. **DatePicker** - เลือกวันที่

```php
DatePicker::make('birth_date')
    ->label('วันเกิด')
    ->required()
    ->displayFormat('d/m/Y')
    ->native(false)
    ->minDate(now()->subYears(65))
    ->maxDate(now()->subYears(18))
```

**คุณสมบัติ:**
- `->displayFormat('d/m/Y')` - รูปแบบการแสดงผล
- `->native(false)` - ใช้ date picker ของ Filament (ไม่ใช่ native browser)
- `->minDate()` - วันที่เริ่มต้นที่เลือกได้
- `->maxDate()` - วันที่สิ้นสุดที่เลือกได้

**ประเภทข้อมูล:** `string` (Y-m-d format)

---

#### 3. **Select** - เลือกจากรายการ

```php
Select::make('gender')
    ->label('เพศ')
    ->options([
        'male' => 'ชาย',
        'female' => 'หญิง',
        'other' => 'อื่นๆ',
    ])
    ->required()
    ->native(false)
    ->searchable()
    ->preload()
    ->multiple() // เลือกได้หลายค่า
    ->live() // อัปเดตแบบ real-time
```

**คุณสมบัติ:**
- `->options([])` - รายการตัวเลือก
- `->native(false)` - ใช้ select ของ Filament (ไม่ใช่ native browser)
- `->searchable()` - ค้นหาได้
- `->preload()` - โหลดข้อมูลล่วงหน้า
- `->multiple()` - เลือกได้หลายค่า (return เป็น array)
- `->live()` - อัปเดตแบบ real-time

**ประเภทข้อมูล:** `string` (single), `array` (multiple)

---

#### 4. **Textarea** - รับข้อความหลายบรรทัด

```php
Textarea::make('address')
    ->label('ที่อยู่')
    ->required()
    ->rows(4)
    ->maxLength(500)
    ->placeholder('กรุณากรอกที่อยู่')
```

**คุณสมบัติ:**
- `->rows(4)` - จำนวนบรรทัดที่แสดง
- `->maxLength(500)` - จำนวนตัวอักษรสูงสุด

**ประเภทข้อมูล:** `string`

---

#### 5. **Radio** - เลือกตัวเลือกเดียว (แสดงเป็นปุ่ม)

```php
Radio::make('work_experience')
    ->label('มีประสบการณ์หรือไม่')
    ->options([
        'yes' => 'มีประสบการณ์',
        'no' => 'ไม่มีประสบการณ์',
    ])
    ->required()
    ->inline() // แสดงในแนวนอน
    ->live() // อัปเดตแบบ real-time
```

**คุณสมบัติ:**
- `->options([])` - รายการตัวเลือก
- `->inline()` - แสดงในแนวนอน (ไม่ใช่แนวตั้ง)
- `->live()` - อัปเดตแบบ real-time

**ประเภทข้อมูล:** `string`

---

#### 6. **Toggle** - สวิตช์เปิด/ปิด

```php
Toggle::make('has_vehicle')
    ->label('มีรถยนต์ส่วนตัว')
    ->default(false)
    ->live() // อัปเดตแบบ real-time
```

**คุณสมบัติ:**
- `->default(false)` - ค่าเริ่มต้น
- `->live()` - อัปเดตแบบ real-time

**ประเภทข้อมูล:** `boolean` (true/false)

---

#### 7. **Checkbox** - ช่องทำเครื่องหมาย

```php
Checkbox::make('agree_terms')
    ->label('ยอมรับข้อกำหนด')
    ->required()
    ->default(false)
```

**คุณสมบัติ:**
- `->default(false)` - ค่าเริ่มต้น
- `->required()` - ต้องเลือก

**ประเภทข้อมูล:** `boolean` (true/false)

---

## 📄 Form 2: ข้อมูลเพิ่มเติม (Additional Information)

### Components ที่ใช้:

#### 1. **FileUpload** - อัปโหลดไฟล์

```php
FileUpload::make('resume')
    ->label('Resume / CV')
    ->acceptedFileTypes(['application/pdf', 'application/msword'])
    ->maxSize(5120) // 5MB
    ->directory('job-applications/resumes')
    ->visibility('private')
    ->downloadable()
    ->previewable()
    ->openable()
    ->multiple() // อัปโหลดได้หลายไฟล์
    ->required()
```

**คุณสมบัติ:**
- `->acceptedFileTypes([])` - ประเภทไฟล์ที่ยอมรับ
- `->maxSize(5120)` - ขนาดไฟล์สูงสุด (KB)
- `->directory('path')` - โฟลเดอร์ที่เก็บไฟล์
- `->visibility('private')` - การเข้าถึงไฟล์ (private/public)
- `->downloadable()` - ดาวน์โหลดได้
- `->previewable()` - ดูตัวอย่างได้
- `->openable()` - เปิดไฟล์ได้
- `->multiple()` - อัปโหลดได้หลายไฟล์

**ประเภทข้อมูล:** `array` (file paths)

---

#### 2. **Select (Multiple)** - เลือกได้หลายค่า

```php
Select::make('preferred_work_days')
    ->label('วันทำงานที่ต้องการ')
    ->options([
        'monday' => 'จันทร์',
        'tuesday' => 'อังคาร',
        // ...
    ])
    ->multiple() // เลือกได้หลายค่า
    ->searchable()
    ->preload()
```

**คุณสมบัติ:**
- `->multiple()` - เลือกได้หลายค่า (return เป็น array)

**ประเภทข้อมูล:** `array`

---

## 🔀 Conditional Display (->visible())

### หลักการ:

ใช้ `->visible()` เพื่อแสดง field ตามเงื่อนไข โดยใช้ `$get()` เพื่ออ่านค่าจาก field อื่น

```php
TextInput::make('years_of_experience')
    ->label('จำนวนปีที่ทำงาน')
    ->visible(fn ($get) => $get('work_experience') === 'yes')
    ->required(fn ($get) => $get('work_experience') === 'yes')
```

**ตัวอย่างการใช้งาน:**

1. **แสดงเมื่อเลือกค่าใน Select/Radio:**
```php
TextInput::make('student_id')
    ->visible(fn ($get) => $get('type') === 'student')
```

2. **แสดงเมื่อเปิด Toggle:**
```php
TextInput::make('reference_name')
    ->visible(fn ($get) => $get('has_references') === true)
```

3. **แสดงเมื่อมีค่า:**
```php
Textarea::make('current_address')
    ->visible(fn ($get) => $get('address') !== null)
```

---

## ⚡ Real-time Updates (->live())

### หลักการ:

ใช้ `->live()` เพื่อให้อัปเดตค่าแบบ real-time เมื่อผู้ใช้เปลี่ยนค่า

```php
Select::make('nationality')
    ->live() // อัปเดตแบบ real-time
    ->debounce(500) // รอ 500ms ก่อนอัปเดต
```

### Livewire Hooks:

เมื่อใช้ `->live()` และ property เปลี่ยน Livewire จะเรียก method อัตโนมัติ:

```php
// เมื่อ $has_references เปลี่ยน จะเรียก method นี้
public function updatedHasReferences(): void
{
    if ($this->has_references) {
        // Logic เมื่อเปิด toggle
    }
}
```

**Naming Convention:** `updated{PropertyName}()`
- Property: `has_references` → Method: `updatedHasReferences()`
- Property: `work_experience` → Method: `updatedWorkExperience()`

---

## 📊 การส่งค่า (Submit)

### Form 1: ข้อมูลส่วนตัว

```php
public function submitPersonalInfo(): void
{
    // อ่านค่าจาก properties
    $name = $this->first_name;
    $email = $this->email;
    
    // หรือรวมเป็น array
    $data = [
        'first_name' => $this->first_name,
        'email' => $this->email,
        // ...
    ];
    
    // แสดง notification
    \Filament\Notifications\Notification::make()
        ->title('บันทึกสำเร็จ!')
        ->success()
        ->send();
}
```

### Form 2: ข้อมูลเพิ่มเติม

```php
public function submitAdditionalInfo(): void
{
    // อ่านค่าจาก properties
    $workHistory = $this->work_history;
    $resume = $this->resume; // array of file paths
    
    // ...
}
```

### Submit ทั้ง 2 Form พร้อมกัน

```php
public function submitAll(): void
{
    $allData = [
        'personal_info' => [
            'first_name' => $this->first_name,
            // ...
        ],
        'additional_info' => [
            'work_history' => $this->work_history,
            // ...
        ],
    ];
    
    // บันทึกลง database หรือส่งอีเมล
    // ...
}
```

---

## 📋 สรุป Components และประเภทข้อมูล

| Component | ประเภทข้อมูล | ใช้สำหรับ |
|-----------|-------------|-----------|
| **TextInput** | string, int, float | ข้อความสั้นๆ, ตัวเลข, อีเมล, เบอร์โทร |
| **Textarea** | string | ข้อความหลายบรรทัด |
| **Select** | string (single), array (multiple) | เลือกจากรายการ |
| **DatePicker** | string (Y-m-d) | เลือกวันที่ |
| **Radio** | string | เลือกตัวเลือกเดียว (ปุ่ม) |
| **Toggle** | boolean | สวิตช์เปิด/ปิด |
| **Checkbox** | boolean | ช่องทำเครื่องหมาย |
| **FileUpload** | array | อัปโหลดไฟล์ |

---

## 🎓 หลักการสำคัญ

### 1. การประกาศ Properties

```php
public ?string $first_name = null;  // TextInput, Textarea
public ?int $years_of_experience = null;  // TextInput (numeric)
public bool $has_vehicle = false;  // Toggle, Checkbox
public ?string $gender = null;  // Select, Radio
public ?string $birth_date = null;  // DatePicker
public ?array $resume = null;  // FileUpload
public ?array $preferred_work_days = null;  // Select (multiple)
```

### 2. การ Bind Component กับ Property

```php
TextInput::make('first_name')  // ต้องตรงกับชื่อ property
    ->label('ชื่อ')
    ->required()
```

### 3. Two-Way Data Binding

Filament ใช้ Livewire ทำให้มี Two-way binding:
- เมื่อผู้ใช้กรอกข้อมูล → ค่าจะถูกส่งไปยัง property อัตโนมัติ
- เมื่อ property เปลี่ยน → ค่าจะถูกแสดงใน form อัตโนมัติ

### 4. Validation

Filament จัดการ validation อัตโนมัติตาม rules ที่กำหนด:
- `->required()` - ต้องกรอก
- `->email()` - ต้องเป็นอีเมล
- `->numeric()` - ต้องเป็นตัวเลข
- `->maxLength(100)` - ความยาวสูงสุด
- `->minValue(0)` - ค่าต่ำสุด
- `->maxValue(100)` - ค่าสูงสุด

### 5. Conditional Logic

```php
// แสดงเมื่อเงื่อนไขเป็นจริง
->visible(fn ($get) => $get('work_experience') === 'yes')

// ต้องกรอกเมื่อเงื่อนไขเป็นจริง
->required(fn ($get) => $get('work_experience') === 'yes')
```

---

## 🚀 การใช้งาน

1. เปิดหน้าเว็บ: `http://f1.test/admin/job-application`
2. กรอกข้อมูลใน Form 1: ข้อมูลส่วนตัว
3. กดปุ่ม "บันทึกข้อมูลส่วนตัว"
4. กรอกข้อมูลใน Form 2: ข้อมูลเพิ่มเติม
5. กดปุ่ม "บันทึกข้อมูลเพิ่มเติม"
6. กดปุ่ม "ส่งใบสมัครงาน" เพื่อส่งทั้ง 2 Form พร้อมกัน

---

## 📚 เอกสารเพิ่มเติม

- [Form Components Guide](./FORM_COMPONENTS_GUIDE.md)
- [Multiple Forms Example](./MULTI_FORM_GUIDE.md)
- [Filament Documentation](https://filamentphp.com/docs)
